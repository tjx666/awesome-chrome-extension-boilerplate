import { hot } from 'react-hot-loader/root';

import './App.scss';

const App = () => (
    <div className="app">
        <h1 className="title">options page</h1>
    </div>
);

export default hot(App);
